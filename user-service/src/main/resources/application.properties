server.port=${SERVER_PORT:0}

spring.application.name=user-service

application-title=User Service

spring.flyway.schemas=${USER_SERVICE_DB_SCHEMA:user_service}
spring.flyway.default-schema=${USER_SERVICE_DB_SCHEMA:user_service}
spring.jpa.properties.hibernate.default_schema=${USER_SERVICE_DB_SCHEMA:user_service}

spring.config.import=optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}